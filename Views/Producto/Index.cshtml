@model IEnumerable<VeterinariaMVC.Models.Producto>

<h2>Lista de Productos</h2>
<a href="@Url.Action("Crear")">Agregar Producto</a>
<table border="1">
    <tr>
        <th>Nombre</th>
        <th>Descripción</th>
        <th>Precio</th>
    </tr>
    @foreach (var producto in Model)
    {
        <tr>
            <td>@producto.Nombre</td>
            <td>@producto.Descripcion</td>
            <td>@producto.Precio</td>
        </tr>
    }
</table>
